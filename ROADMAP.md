# Roadmap

## Current — 0.5.0 release candidate

Implemented in the 0.5 train:
- SRIC/ReproSec 0.5 compatibility.
- Coverage-guided validation planning for `UNKNOWN` authorization cells.
- Explicit resource equivalence classes before cross-resource experiment compression.
- Safe-only representative planning and deterministic cross-tool claim fingerprints.
- CLI/API surfaces and regression tests for validation planning.
- `doctor` compatibility updated for SRIC 0.5 and coordinated release-evidence gate v2.

## Next hardening
- Deterministic browser/proxy observation adapters with explicit actor/session binding.
- Richer tenant/delegation/invitation/ownership-transfer state machines.
- Deeper field-level GraphQL authorization evidence and stateful WebSocket revocation/subscription evidence.
- Large-matrix virtualization and saved research queries in the Web UI.
- Expanded synthetic authorization eval corpus and calibrated differential false-positive benchmarks.

## 1.0
Stable authorization schema/invariant DSL, migration path, external case studies and audited validation workflow.
