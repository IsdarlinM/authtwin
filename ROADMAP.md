# Roadmap

## Current — 0.2.x hardening
Authorization discovery, coverage matrix, counterfactual plans, state/session lifecycle, invariant DSL, GraphQL/batch modeling, Skeptic review, SRIC graph/lineage/jobs and RCAP interoperability are implemented.

Next:
- Deterministic browser/proxy observation adapters with explicit actor/session binding.
- Richer tenant/delegation/invitation/ownership-transfer state machines.
- Field-level GraphQL authorization evidence and WebSocket authorization observations.
- Large-matrix virtualization and saved research queries in the Web UI.
- Expanded synthetic authorization eval corpus and differential false-positive benchmarks.

## 1.0
Stable authorization schema/invariant DSL, migration path, external case studies and audited validation workflow.
